<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "ItCounts" %></title>
    <%= stylesheet_link_tag  "http://fonts.googleapis.com/css?family=Montserrat" %>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <script src="//cdn.jsdelivr.net/velocity/1.2.2/velocity.min.js"></script>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="row">
      <header class="large-12 columns">
        <div class="left">
          <nav id="nav-btn">
            <button class="navbar-toggle collapsed" type="button" id="pull" data-toggle="collapse" data-target=".bs-navbar-collapse">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </nav>
          <div class="header-logo">
          <!-- this is the anchor tag for itcounts -->
            <a href="/">
              <h1>itcounts</h1>
              <img src="<%= asset_path( 'logo.svg' ) %>">
            </a>
          </div>
        </div>

        <% if logged_in? %>
        <div class="login-btn right">
          <button><a href="/logout">Logout</a></button>
        </div>
        <% else %>
        <div class="login-btn right">
          <button><a href="/login">Login</a></button>
        </div>
        <% end %>

      </header>


       </div>
    <ul id="primary-nav">
      <a href="/"><li>Home</li></a>
      <a href="/categories"><li>Categories</li></a>
      <a href="/contact"><li>Contact</li></a>
    </ul>

    <div id="breadcrumbs">
      <%= render_breadcrumbs :separator => ' <em>></em> ' %>
    </div>
    <div class="row">
      <main>
        <%= yield %>
        <section class="large-12 columns footer-section">
          <div class="statement">
            <p>Start where you are. Use what you have. Do what you can. Now change the world.</p>
          </div>
          <div class="social-btn small-6 column">
            <ul>
              <li><a href=""><%=  inline_svg("social_network/facebook24.svg") %></a></li>
              <li><a href=""><%=  inline_svg("social_network/linkedin12.svg") %></a></li>
              <li><a href=""><%=  inline_svg("social_network/twitter47.svg") %></a></li>
              <li><a href=""><%=  inline_svg("social_network/google2.svg") %></a></li>
            </ul>
          </div>
          <aside class="small-6 column">
            <p>Sponsored by:</p>
            <img src="<%= asset_path( 'social_network/ga.png' )  %>">
          </aside>
        </section>
      </main>

    </div>




    <footer class="large-12 columns">

      <div class="bottom">
        <p class="small-8 column">App Development - Made in Melbourne</p>
        <p class="small-4 column copyright">© MVP 2015</p>
      </div>

    </footer>
    <script>
      $(function() {
        var pull    = $('#nav-btn');
          menu    = $('#primary-nav');
          menuHeight  = menu.height();

        $(pull).on('click', function(e) {
          e.preventDefault();
          menu.slideToggle(400);
        });

      });
    </script>
  </body>
</html>
